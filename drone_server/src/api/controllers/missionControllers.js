const prisma = require('../../../prisma/index');

// create new mission

exports.createMission = async(req, res, next) => {
    const {missionType, description, pilotId} = req.body;
    try {
        
    }
    catch(error){

    }
}